<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bbkk.luckygo_pemandu">

    <!-- ===================== Permissions ===================== -->
    <!-- Network / Internet (required for Flutter, Firebase, Jitsi) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Location (your existing map features) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Camera & Mic (Jitsi: mic for voice; camera is optional) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- Audio routing & Bluetooth for headsets (Jitsi calls) -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <!-- Legacy Bluetooth (pre-Android 12) -->
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <!-- Android 12+ runtime Bluetooth permission for audio routing to BT devices -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>

    <!-- Keep CPU awake during an ongoing call if needed -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Android 13+ media permission (you had this already) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <!-- Legacy external storage read (limit to API <= 32) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <!-- Declare that camera/microphone hardware are optional, not required to install -->
    <uses-feature android:name="android.hardware.camera.any" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>

    <!-- ================= Package Visibility (Android 11+) ================= -->
    <queries>
        <!-- Google Maps app -->
        <package android:name="com.google.android.apps.maps" />
        <!-- Google Play Store (fallback when Maps isn't installed) -->
        <package android:name="com.android.vending" />

        <!-- Intents we plan to resolve for navigation -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="google.navigation" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="comgooglemaps" />
        </intent>

        <!-- Keep your existing text processing query -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

    <!-- ===================== Application ===================== -->
    <application
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:label="Driver">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBr2bdTwwE-x9PyHSArUbCgZ_BsDupqmfA" />

        <!-- Main Flutter Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>

</manifest>
